fn a()
    throw Error("RuntimeError", "foo")
end

fn call_a()
    try
        a()
     catch e : int # 故意使其匹配不到错误类型
          print("666")
     end
end

try
    call_a()
catch e: str
    print("error is str")
catch e: Error
    print(e.k) # 故意使其报错
end
