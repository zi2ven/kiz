clock = now()

fn fib_recursive(n)
    if n < 2
        return n
		else
	      return fib_recursive(n-1) + fib_recursive(n-2)
		end
end

print("fib(30)=", fib_recursive(30))
print("using", now()-clock, "ns")